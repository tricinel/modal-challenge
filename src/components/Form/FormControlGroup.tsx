import styled from '@emotion/styled';
import { space } from '../../tokens';
import Box from '../Box';

const FormControlGroup = styled(Box.withComponent('div'))`
  margin: 0 0 ${space.lg} 0;
`;

export default FormControlGroup;
